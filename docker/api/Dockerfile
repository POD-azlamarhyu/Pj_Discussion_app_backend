FROM eclipse-temurin:21.0.7_6-jdk
ARG API_PORT
ARG LOCALDEV_WORKDIR
ARG JAVA_CONTAINER_USER
ARG JAVA_CONTAINER_USER_ID
ARG APP_NAME
ARG BUILD_WORKDIR

WORKDIR ${LOCALDEV_WORKDIR}
RUN apt-get update && apt-get upgrade -y
# COPY . ${LOCALDEV_WORKDIR}
EXPOSE ${API_PORT}
